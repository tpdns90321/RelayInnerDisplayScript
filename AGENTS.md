READ FIRST README.md, AND READ SPECIFIED '*.specs' FILES.

When start work for implementing spec to real, Make branch for spec and use `git worktree` from this branch to 'tasks/spec-*' folder. Also, Use top project's one instead of its workspace's 'tasks/{lessons,tasks}.md', when need it. And when completed implementation, update README.md to current state.
